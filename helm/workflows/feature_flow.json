{
  "id": "feature_flow",
  "name": "Feature Delivery Flow",
  "description": "Plan and execute a feature request against a primary application repository.",
  "entrypoint": "plan",
  "metadata": {
    "default_repo": "airteam/app",
    "outputs_required": ["branch", "summary", "pr_url"]
  },
  "steps": [
    { "id": "plan", "name": "Plan Work", "type": "plan" },
    {
      "id": "execute_app",
      "name": "Implement Application Changes",
      "type": "execute",
      "repo": "airteam/app",
      "instructions": "Implement the requested feature in the main application repository.",
      "requires": ["plan"]
    },
    { "id": "result", "name": "Produce Result", "type": "result", "requires": ["execute_app"] }
  ]
}
