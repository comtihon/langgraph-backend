apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "langgraph-backend.fullname" . }}-workflows
data:
{{- range $path, $bytes := .Files.Glob "workflows/*.json" }}
  {{ base $path }}: |
{{ $bytes | toString | indent 4 }}
{{- end }}
