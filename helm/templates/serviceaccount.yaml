apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "langgraph-backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "langgraph-backend.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
